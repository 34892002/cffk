# CFFK vike-cf 重构进度

> 最后更新：2026-08-06
>
> 状态说明：
> - `[x]` 已实现并完成对应验证
> - `[~]` 已有代码或骨架，但仍缺少真实联调、完整测试或生产能力
> - `[ ]` 尚未实现

## 总体状态

当前项目已经完成新框架基础、认证、商品目录、卡密库存、订单核心、支付宝支付骨架和支付结果页。

当前仍不能宣称生产完成，主要缺口是：

1. 支付宝真实环境联调和真实 RSA2 密钥测试。
2. D1 回调幂等、库存、优惠码和卡密并发测试。
3. 邮件真实发送适配器及真实 Worker binding 联调。
4. 支付、库存、优惠码并发安全回归测试。
5. S3、Cron、端到端回归测试。

---

## 一、项目基础与技术栈

### 已完成

- [x] 以 `vike-cf` 作为全新项目基础，不迁移 `edgeKey` 历史数据。
- [x] 使用 Vike、Vue、Hono、Cloudflare Workers、D1、Drizzle、Better Auth、Telefunc 和 shadcn-vue。
- [x] 使用 Drizzle 替代 Prisma。
- [x] 保留新的 shadcn-vue UI，不继续沿用旧项目 UI。
- [x] EdgeKey 不再作为新项目的运行时依赖。
- [x] 建立 Telefunc 统一请求与错误处理基础：`AppError`、服务端 context guard、客户端 `runTelefunc()` 和全局 Sonner。

### Telefunc 与前端反馈规范

- [x] 新 Telefunc 成功时直接返回业务数据；预期业务失败抛出稳定大写错误码（例如 `PRODUCT_STOCK_NOT_ENOUGH`），不使用 `{ ok, data, error }` 包装。
- [x] 新 Telefunc 优先使用 `requireDatabase()`、`requireUser()`、`requireAdmin()` 获取上下文和权限。
- [x] 不向浏览器返回数据库、Worker Secret、支付渠道或第三方服务的原始异常。
- [x] `runTelefunc()` 统一处理 Sonner 成功/通用失败提示并继续抛出异常；关键页面仍用 `Alert` 保留可操作的错误上下文。
- [x] 字段级校验错误在表单字段或表单顶部展示；不以短暂 toast 替代关键订单、支付、发货或登录失败信息。

### 迁移与初始化

- [x] 生成单一初始 migration：`database/migrations/0000_orange_shinko_yamashiro.sql`。
- [x] 初始 migration 已包含 Better Auth（含用户名）、首位管理员引导、商品与库存、订单交付唯一索引、优惠码预占、支付、邮件重试、媒体及 S3 配置全部 22 张表。
- [x] 已删除本地 `.wrangler` 数据并完成首次安装验证：单一 migration 执行 58 条建表/索引语句，随后 seed 执行 5 组初始业务配置。
- [x] `npm run db:check` 已通过。
- [x] 新项目初始发布前允许重建这一个 migration；首次部署后 schema 变更只能新增 migration，不修改该历史文件。 

### `scripts/seed.sql` 处理方式

- [x] 保留 `scripts/seed.sql`，作为全新环境的初始业务配置脚本，不作为 schema migration。
- [x] seed 只幂等插入默认站点设置、支付宝占位配置和邮件模板。
- [x] seed 不创建管理员，不保存密码、支付私钥、SMTP 密码或 API Key。
- [x] 新环境执行顺序：先 migration，再 seed。
- [x] 支付和邮件敏感值通过 `wrangler secret put <NAME>` 设置。
- [x] 数据库 JSON 只保存 `{ "secret": "WORKER_SECRET_NAME" }` 引用。

---

## 二、认证与管理员安全

### 已完成

- [x] 使用 Better Auth 用户、Session、Account 和 Verification 表。
- [x] `user.username` 是唯一后台登录标识，语义对齐 EdgeKey 的 `Admin.username`；`user.name` 用作昵称，密码哈希由 Better Auth 的 `account` 表维护。
- [x] 后台登录使用用户名和密码；注册页要求用户名并创建对应 Better Auth 账户。
- [x] 使用 `adminProfile` 表实现管理员身份，与用户身份分离。
- [x] 使用 `adminBootstrap` 单例无竞争选举首位注册管理员。
- [x] 首位成功注册用户获得 `ACTIVE` 管理员权限。
- [~] 本地 D1 已因用户名 schema 重置；需以用户名重新注册首位管理员后进行浏览器级登录验证。 
- [x] 管理后台隐藏路径必须匹配 `ADMIN_PATH`。
- [x] 后台页面只允许已登录且 `ACTIVE` 管理员访问。
- [x] 修复 CDN/CNAME 回源下的 Better Auth 公开 Origin、Cookie、回调和缓存策略。
- [x] 认证响应返回 `Cache-Control: private, no-store`，避免 CDN 缓存认证结果。
- [x] 已验证管理员访问 `/admin888/dash`。
- [x] 已验证非管理员不能访问后台。
- [x] 登录页有提交状态、异常捕获和无管理员权限提示。

### 未完成

- [~] 管理员状态管理页面：可查看管理员、启停其他管理员、禁止修改自己；停用更新带有至少两名活动管理员的原子 SQL 条件，避免并发锁死后台。待浏览器级回归验证。
- [ ] 管理员邀请机制。
- [ ] 关闭公开注册或改为邀请注册。
- [ ] 2FA 完整启用流程。
- [ ] 后台登录失效、Session 过期和无授权场景的浏览器级回归测试。

---

## 三、商品目录与库存

### 商品目录

- [x] 首页 SSR 读取 D1 真实商品目录。
- [x] 只展示已上架商品。
- [x] 只展示启用分类和非敏感站点信息。
- [x] 商品详情页 `/product/:slug`。
- [x] 商品详情包含价格、数量限制、库存、交付方式和购买说明。
- [x] 未找到商品返回 `404`。
- [x] 不向前台暴露卡密内容。
- [x] 商品详情页已连接 `/checkout?product=<slug>`。

### 商品后台

- [x] 分类读取、新建、编辑、启停。
- [x] 商品读取、新建、编辑、上下架。
- [x] Telefunc mutation 重新校验管理员权限。
- [x] 商品和分类操作写入管理员审计日志。

### 卡密库存

- [x] 卡密导入，仅允许用于 `CARD_AUTO` 商品。
- [x] 单次导入最多 1,000 行。
- [x] 按商品、状态筛选和分页。
- [x] 启用/禁用未使用卡密。
- [x] 卡密状态流转：`UNUSED -> LOCKED -> SOLD`。
- [x] 条件更新避免重复预占。
- [x] 订单失败或关闭时释放当前订单锁定的卡密。
- [x] 卡密库存数量只统计 `UNUSED`。
- [x] 卡密内容不进入公开目录和库存数量接口。

### 未完成

- [ ] 已登录管理员的卡密导入、筛选、启停浏览器级 Telefunc 回归测试。
- [ ] 卡密并发购买测试。
- [x] 商品详细描述、固定发货内容、人工/物流交付提示、购买说明、联系方式/库存可见开关已可维护；`FIXED_CARD` 商品上架前强制填写固定交付内容。商品和分类采用停用归档而非物理删除；停用分类前必须先下架其上架商品，公开目录也只显示启用分类下的上架商品。

---

## 四、订单、结算与交付

### 订单核心

- [x] 商品上架状态校验。
- [x] 购买数量边界校验。
- [x] 商品名称和价格快照。
- [x] 联系方式校验。
- [x] 物流订单收件信息校验。
- [x] 优惠码有效期、最低金额、商品范围和使用上限校验。
- [x] 未支付订单优惠码 `reservedCount` 预占。
- [x] 支付成功后优惠码转为 `usedCount`。
- [x] 自动卡密预占。
- [x] 人工/物流订单实物库存条件扣减。
- [x] 零元订单自动确认支付并交付。
- [x] 待支付订单关闭和资源释放。
- [x] 支付请求创建失败时关闭订单并释放资源。

### 结算与订单查询

- [x] 结算页 `/checkout?product=<slug>`。
- [x] 数量、联系方式、收件信息、优惠码和备注表单。
- [x] 无支付渠道时只允许零元订单。
- [x] 有效支付宝配置启用后支持非零订单支付创建。
- [x] 订单查询页 `/order`。
- [x] 订单号和查询令牌必须同时匹配。
- [x] 交付内容只在令牌验证通过后返回。
- [x] 支付结果页 `/payment-result`。
- [x] 支付结果页不信任支付宝回跳本身，重新从 D1 查询订单状态。

### 发货一致性

- [x] 已支付订单不会因临时交付写入失败被改为支付失败。
- [x] 重复支付回调会重新尝试交付。
- [x] 每个订单最多一条成功交付快照。
- [x] 自动卡密交付支持从 `LOCKED`/`SOLD` 关联卡密恢复。
- [x] 人工发货后台：记录交付内容、写入成功快照、更新订单状态和审计日志。
- [x] 物流发货后台：记录物流或发货内容、写入成功快照、更新订单状态和审计日志。
- [x] 订单后台：订单号、订单状态、交付状态筛选；查看订单、支付日志与交付快照；关闭待支付订单。
- [x] 订单交付失败恢复入口：自动交付重试；人工/物流订单可标记失败后修正并再次交付。
- [~] 人工/物流交付成功和管理员标记失败已接入邮件事件，真实 Email binding 联调仍未完成。
- [ ] 订单、库存、优惠码并发测试。

---

## 五、支付系统

### 通用支付设计

- [x] 支付渠道统一存储在 `paymentProvider`。
- [x] Provider 配置使用 `configJson`。
- [x] Provider 启用状态由 `isEnabled` 控制。
- [x] 前台只读取已启用且配置校验通过的渠道公开信息。
- [x] 支付密钥不保存在数据库，只引用 Worker Secret。
- [x] 支付日志存储在 `paymentLog`。

### 支付宝

- [x] 网页/H5 与当面付合并为单一 `ALIPAY` Provider。
- [x] `configJson.mode = "web"` 生成网页支付链接。
- [x] `configJson.mode = "face_to_face"` 调用 `alipay.trade.precreate`。
- [x] 使用 Worker Web Crypto 实现 RSA2 签名和验签。
- [x] 回调校验签名、应用 ID、交易状态、订单号和金额。
- [x] 回调确认支付并触发交付；只有 `PENDING + UNPAID` 订单可首次确认，已关闭/失败订单的迟到通知返回失败，已支付订单保持幂等交付重试。
- [x] 回调写入 `paymentLog`。
- [x] 支付回跳使用订单号和查询令牌查询真实状态。

### 支付后台与路由

- [x] 后台支付渠道页 `/admin888/system/payments`。
- [x] 支付创建 Telefunc。
- [x] 支付宝回调路由 `/api/payments/alipay/notify`。
- [x] 支付结果页 `/payment-result`。
- [x] 空回调已验证返回 `400 failure`。

### 未完成

- [ ] 真实支付宝沙箱/生产联调。
- [ ] 真实 RSA2 密钥签名样本测试。
- [x] 当面付二维码视觉渲染：使用本地 `qrcode` canvas 生成，不依赖第三方二维码服务；保留支付链接作为备用路径。
- [ ] Epay 支付创建和回调。
- [ ] BEpusdt 支付创建和回调。
- [ ] Stripe 支付创建和 Webhook 验证。
- [ ] HashPay 支付创建和回调。
- [~] 支付回调状态机已覆盖首次确认、已支付幂等重试和关闭/失败订单拒绝；仍缺 D1 重复通知与并发集成测试。

---

## 六、邮件系统

### 已完成

- [x] 邮件 Provider 使用 JSON 配置。
- [x] 邮件模板使用 JSON 配置。
- [x] `{{variable}}` 模板变量渲染。
- [x] 收件人和模板配置校验。
- [x] 未实现发送或发送失败时写入 `emailLog`。
- [x] 邮件 Provider 后台：读取、启停、JSON 配置保存和审计日志。
- [x] 邮件模板后台：读取、启停、JSON 配置保存和审计日志。
- [x] 邮件历史后台：按发送状态筛选、分页查看成功消息 ID 或失败原因。
- [x] 管理员测试邮件入口；使用当前启用 Provider 和 `TEST` 模板，结果写入历史。
- [x] `npm test` 已覆盖邮件模板渲染。

### 未完成

- [ ] SMTP 发送适配器。
- [ ] HTTP API 发送适配器。
- [~] Cloudflare Email Sending 适配器已实现，仍需在真实 Worker Email binding 下联调。
- [~] 支付成功、自动发货成功、人工/物流发货成功及发货失败均已接入邮件事件；发送失败不影响订单支付或交付，真实 Email binding 与失败重试尚未验证。
- [~] 邮件发送失败重试：`emailRetry` 保存渲染后的消息和 Provider 配置快照；Cron 采用指数退避重试，最多 5 次，并为每次重试写入独立历史。仍需真实 Email binding 下的发送与重试联调。

---

## 七、媒体与 S3

### 已完成

- [x] 建立 `media` 和 `s3Config` schema。
- [x] S3 配置设计为 JSON，并支持 Secret 引用示例。
- [x] S3 配置后台：校验 endpoint、region、bucket、Secret 引用和可选公开 URL；单例配置保存会写入管理员审计日志。
- [x] `scripts/seed.sql` 幂等创建不含真实凭据的 S3 配置占位记录。

### 未完成

- [~] S3 图片上传：支持 JPEG、PNG、WebP、GIF，单文件限制 10MB，使用 Worker runtime Secret 进行 SigV4 签名；待真实 S3/R2 联调。
- [~] S3 访问 URL 生成：支持 `publicBaseUrl`、virtual-host 与 path-style URL；待真实桶与 CDN 验证。
- [~] S3 删除和媒体记录清理：对象删除成功或返回 `404` 后删除媒体记录；待真实 S3/R2 联调。
- [~] 媒体后台已实现 S3 配置、图片上传、预览列表和删除；待真实 S3/R2 联调。

---

## 八、后台与运营功能

### 已完成

- [x] 仪表盘使用 D1 实时数据，不再使用收入 mock。
- [x] 商品目录后台。
- [x] 卡密库存后台。
- [x] 支付渠道后台。
- [~] 管理员状态后台：管理员列表和启停已实现，待浏览器级回归验证。
- [x] 后台路由层级已整理：`/dash` 仅为仪表盘；商品、卡密、订单、优惠码、支付、管理员和媒体为 `/admin888/<资源>`；推送模块统一为 `/admin888/push/<资源>`。
- [x] 推送后台采用统一路由：`/push/config` 管理规则，`/push/history` 查看全部渠道日志，`/push/email/*` 管理邮件统计、通道和模板；企业微信与 Telegram 使用 `/push/<渠道>`。
- [x] 移除遗留 `mock.json`，任务页改为真实空状态。

### 未完成

- [~] 后台页面与 Telefunc 已落地：仪表盘、商品目录、卡密库存、订单、优惠码、支付渠道、管理员、媒体存储，以及推送配置、发送日志、邮件统计、通道、模板和测试发送。核心操作仍待浏览器级回归验证。
- [~] 优惠码管理后台：创建、编辑、启停、适用商品、用量和有效期管理已实现，待浏览器级回归验证。
- [x] 订单管理后台：筛选、分页、订单明细、支付日志、交付快照、关闭待支付订单和自动交付重试。
- [x] 手动发货后台：交付内容、成功/失败状态、审计日志和失败后再次交付。
- [x] 物流发货后台：物流/发货内容、成功/失败状态、审计日志和失败后再次交付。
- [x] 推送配置、统一发送日志、邮件统计、Provider 配置、模板和测试发送后台；推送模块仅使用 `/push/*` 路径。
- [~] S3/媒体后台已实现配置、图片上传、预览和删除；待真实 S3/R2 联调。
- [~] 管理员状态页面已实现，邀请和 2FA 页面尚未实现。
- [ ] 任务/运维页面：目前仅展示真实空状态，尚无任务执行记录、日志、告警或人工重试管理能力。

---

## 九、定时任务与运维

### 已完成

- [x] `closePendingOrder()` 订单关闭和资源释放服务已实现。
- [x] Worker `scheduled` handler 每 10 分钟执行一次，关闭创建超过 30 分钟的 `PENDING + UNPAID` 订单，并复用条件关闭和资源释放逻辑。
- [x] Wrangler 使用 `@cloudflare/vite-plugin` 提供的 Vike 虚拟入口 `vike:server-entry`，不将 `dist/server/index.mjs` 作为开发 Worker 源码入口；`npm run deploy -- --dry-run` 已通过。
- [x] 本地 migration 和 seed 命令已配置。
- [x] 构建、Drizzle schema 检查和纯函数测试命令已配置。

### 未完成

- [~] Cron 自动关闭过期订单已实现，仍需在已部署 Worker 中验证实际触发与 D1 资源释放结果。
- [~] Cron 触发邮件重试已实现，仍需在已部署 Worker 和真实 Email binding 下验证。
- [ ] 运行日志、失败告警和运维页面。
- [ ] 生产环境部署验证。

---

## 十、测试与验证

### 已验证

- [x] `npm test`：当前 6 项纯函数测试通过（包括支付确认状态机与邮件重试指数退避上限）。
- [x] `npm run db:check` 通过。
- [~] 单一初始 migration 已生成并通过 schema 检查；本地 D1 首次安装验证待停止当前开发服务器后执行。
- [x] `npm --prefix vike-cf run build` 通过。
- [x] `npm run deploy -- --dry-run`：Vike Worker 入口、静态资源、D1 绑定和 Cron 配置通过 Wrangler 打包验证，未实际部署。
- [x] 未登录访问后台会被重定向。
- [x] `/order` 返回 `200`。
- [x] 无效商品详情返回 `404`。
- [x] 无效结算商品返回 `404`。
- [x] 空支付宝回调返回 `400 failure`。

### 未验证

- [ ] 浏览器级管理员登录完整流程。
- [ ] 浏览器级商品、卡密、订单、优惠码、支付、管理员、邮件和媒体后台 Telefunc 操作。
- [ ] 真实支付宝网页支付。
- [ ] 真实支付宝当面付。
- [ ] 真实 RSA2 回调验签。
- [ ] 重复支付回调并发处理。
- [ ] 卡密、实物库存和优惠码并发购买。
- [~] 零元订单、支付宝回调后的邮件事件路径和失败重试队列已构建验证；真实 Email binding 发送与失败重试未验证。
- [~] S3 上传、公开 URL 和删除流程已构建验证；待真实 S3/R2 桶、Worker Secret 和 CDN 联调。
- [~] 已实现的订单过期 Cron 尚未在已部署 Worker 中实际触发验证。
- [ ] 全流程 E2E。

---

## 初始化和部署规则

1. 新环境先执行 migration，再执行 seed：
   - `npm run db:migrate:local`
   - `npm run db:seed:local`
2. 远程环境使用对应的 `--remote` 命令。
3. `scripts/seed.sql` 是幂等业务初始配置，不是 migration，也不应替代 migration。
4. seed 不创建管理员，不写入密码和任何真实密钥。
5. 第一个成功注册用户成为首位管理员；正式部署前应关闭公开注册或改为邀请机制。
6. 敏感值通过 `wrangler secret put <NAME>` 写入 Worker Secret。
7. 数据库 JSON 只保存 Secret 名称引用。
8. 使用 CDN/CNAME 时，`siteSetting.siteUrl` 必须配置为认证使用的公开 HTTPS Origin。
9. 认证响应必须保持 `Cache-Control: private, no-store`。
10. schema 已应用后只能新增 migration，不修改历史 migration。
