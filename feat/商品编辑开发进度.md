# CFFK 商品编辑开发进度

> **当前状态：核心功能已实现，待管理接口专项测试和浏览器/Workers 验收**
>
> 详细设计与接口契约见 [商品编辑设计方案.md](./商品编辑设计方案.md)。实现必须遵循 `docs/dev.md` 和 `docs/components.md`。
>
> 状态约定：`[ ]` 未开始，`[-]` 进行中或部分完成，`[x]` 已完成。只有代码、测试和验证均达到该项说明时才能标记完成。

## 总体进度

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 0. 设计与范围确认 | 已完成 | 已阅读 edgeKey 参考实现和 CFFK 编码规范，固定 Dialog 双模式方案 |
| 1. 商品列表接口与安全投影 | [-] | 已完成服务端筛选、分页、最小列表字段、单商品详情和自动卡密可用库存摘要；待补齐完整验证 |
| 2. 商品表单基础重构 | [-] | 已新增 schema、表单 Dialog、请求拆分和分层失败反馈；待完成浏览器交互验证 |
| 3. 快速添加与完整编辑体验 | [-] | 已完成双模式、金额转换、发货条件、媒体接入、自动卡密库存摘要和 RadioGroup 发货选择；待完善关闭确认与完整手动验收 |
| 4. 商品富文本闭环 | [-] | 已接入 Tiptap、服务端白名单清洗和公开安全渲染；待补齐媒体插入手动验证 |
| 5. 错误处理、安全与测试 | [-] | 已完成富文本 XSS 清洗、服务端边界校验、错误映射/脱敏和鉴权边界测试；真实管理接口 D1 契约测试待补齐 |
| 6. 工程验证与部署验收 | [-] | 全量测试 61 项通过，lint/build/diff 检查通过，相关 diagnostics 无 error；尚未完成浏览器/Workers 实测 |

## 阶段 0：设计与范围确认（7 项）

- [x] **0.1 阅读 edgeKey 新建商品源码**：确认独立 `ProductForm`、发货条件字段、媒体选择和 Tiptap 富文本能力。
- [x] **0.2 阅读 CFFK 当前商品实现**：确认列表、Dialog、商品模型、保存接口和公开详情渲染现状。
- [x] **0.3 阅读 CFFK 编码规范**：以 `docs/dev.md` 和 `docs/components.md` 为强约束。
- [x] **0.4 固定页面边界**：继续使用 `/catalog/products` 唯一正式页面，不增加 `/new` 或 `/:id` 编辑页。
- [x] **0.5 固定交互模型**：新增和编辑复用同一个 `ProductEditorDialog`，支持快速与完整模式。
- [x] **0.6 固定富文本边界**：仅 `description` 使用受限富文本，发货内容、发货提示和购买说明保持纯文本。
- [x] **0.7 输出开发文档**：完成设计方案和本进度列表。

## 阶段 1：商品列表接口与安全投影（8 项）

- [x] **1.1 定义商品列表契约**：新增 `ProductListQuery`、列表投影和 `items/total/page/pageSize/categories` 返回结构。
- [x] **1.2 实现查询参数校验**：服务端校验请求对象、关键词类型、页码/每页数量安全整数，校正范围、截断关键词并严格校验状态和分类参数。
- [x] **1.3 实现服务端筛选与分页**：名称/Slug 关键词、分类、状态和总数均在服务端查询。
- [x] **1.4 收敛列表投影**：列表投影不返回描述、固定发货内容、发货提示和购买说明。
- [x] **1.5 新增单商品管理详情接口**：点击编辑按正整数 ID 加载完整商品，入口复用 root 鉴权。
- [x] **1.6 接入页面服务端分页**：页面仅展示服务端当前页并使用服务端 total/page/pageSize。
- [x] **1.7 规范筛选交互**：关键词、分类和状态由“查询”显式请求，“重置”清空后请求，查询和每页数量均重置到第 1 页。
- [ ] **1.8 验证列表行为**：空状态、刷新、筛选、分页、页码回退和响应式工具栏符合 `AdminDataTable` 规范。

## 阶段 2：商品表单基础重构（10 项）

- [x] **2.1 新增 `product-form.ts`**：已集中定义表单类型、默认值、Zod schema 和数据转换。
- [x] **2.2 接入 VeeValidate**：已使用 `useForm()`、`toTypedSchema()` 和 `Field as VeeField` 管理商品表单。
- [x] **2.3 使用 shadcn-vue Field**：字段使用 `Field`、`FieldLabel`、`FieldDescription`、`FieldError`，并用 `FieldSet` 分组。
- [x] **2.4 提取 `ProductEditorDialog.vue`**：已从 `+Page.vue` 移出 Dialog 和表单展示，页面继续负责请求和业务状态。
- [x] **2.5 统一新建和编辑状态**：使用一个 Dialog、一个 schema 和一套转换函数，通过 `resetForm({ values })` 装载默认值或编辑详情。
- [x] **2.6 实现加载状态**：编辑详情加载和保存期间禁用相应操作，避免重复请求和旧数据闪现。
- [x] **2.7 实现脏状态保护**：`meta.dirty` 时阻止遮罩和 `Escape` 意外关闭。
- [x] **2.8 实现取消确认**：脏表单点击取消会使用项目 Dialog 确认放弃，不使用浏览器 `confirm()`。
- [x] **2.9 统一反馈**：保存和状态切换通过 `runTelefunc()` 显示 Sonner Toast。
- [x] **2.10 保留页面级失败状态**：仅首次无法加载商品列表时显示脱敏 `Alert`；已有列表后的刷新失败和编辑详情失败使用 Toast，不阻断页面操作。

## 阶段 3：快速添加与完整编辑体验（14 项）

- [x] **3.1 增加双模式入口**：已提供快速添加、添加商品和编辑入口，并具备脏表单关闭确认。
- [x] **3.2 实现快速模式字段**：快速模式展示名称、分类、价格、发货方式及适用条件字段。
- [x] **3.3 快速模式固定草稿**：提交转换强制使用 `DRAFT`。
- [x] **3.4 模式切换保留输入**：快速模式切换完整配置保留当前表单输入和校验状态。
- [x] **3.5 完成基本信息分区**：名称、副标题、Slug 和分类使用 FieldSet 组织。
- [x] **3.6 完成 Slug 交互**：新建时自动生成，手动修改后锁定；编辑时保留原值，并显示前台路径预览。
- [x] **3.7 完成商品媒体分区**：封面 URL、`MediaPickerDialog` 和图片预览已接入。
- [x] **3.8 完成价格与购买规则分区**：后台以元输入、接口以分提交，并校验两位小数和安全整数。
- [x] **3.9 完成购买范围校验**：客户端和服务端均保证 `maxBuy >= minBuy >= 1`。
- [x] **3.10 补充或复用 `RadioGroup`**：通过 shadcn-vue CLI 添加 `components/ui/radio-group`，发货方式改为四项说明卡片式单选，不使用原生 radio。
- [x] **3.11 完成发货条件字段**：四种发货方式仅提交各自适用字段。
- [x] **3.12 完成发布设置**：状态、联系方式要求和库存展示使用 Select/Switch 绑定。
- [x] **3.13 接入卡密管理提示**：编辑自动卡密商品时，详情接口仅返回可用数量；弹窗展示摘要并链接既有卡密管理页，新商品提示需先保存。卡密全文不进入商品详情接口或表单。
- [-] **3.14 完成响应式布局**：Dialog 已采用视口高度限制、内容区滚动、Footer 固定和 `sm:grid-cols-2` 卡片布局；仍需浏览器实测宽窄屏行为。

## 阶段 4：商品富文本闭环（11 项）

- [x] **4.1 选择 Workers 兼容清洗方案**：采用有限标签/属性白名单纯函数清洗器，不依赖 JSDOM、浏览器 `window` 或 Node API。
- [x] **4.2 安装最小 Tiptap 依赖**：已安装 Vue、StarterKit、Link、Image 和 Placeholder。
- [x] **4.3 实现 `ProductRichTextEditor.vue`**：已支持正文、H2/H3、粗体、斜体、引用、列表、链接、图片、分割线、撤销、重做和清除格式。
- [x] **4.4 限制编辑能力**：不提供 HTML 源码、颜色、背景、任意字体、iframe、视频、Base64 图片和任意文件附件。
- [x] **4.5 接入媒体图片**：复用 `MediaPickerDialog` 插入 `/media/proxy/*` 图片，编辑器不直接处理上传。
- [x] **4.6 完成编辑器可访问性**：工具栏按钮具有 `aria-label`、title、禁用状态和键盘可操作性。
- [x] **4.7 实现服务端 HTML 清洗**：新增 `server/catalog/product-description.ts`，执行标签、属性和 URL 白名单。
- [x] **4.8 增加富文本长度限制**：限制原始 HTML、清洗后 HTML、URL 和文本属性长度，空文档规范化为 `null`。
- [x] **4.9 保存接口强制清洗**：商品创建和更新均经过服务端清洗。
- [x] **4.10 改造公开商品详情**：使用 `v-html` 渲染已清洗描述，并添加统一富文本排版样式。
- [x] **4.11 展示公开说明字段**：公开展示购买说明和人工/物流发货提示，不公开固定发货内容。

## 阶段 5：服务端校验、错误和安全测试（12 项）

- [-] **5.1 统一商品管理 root 鉴权**：列表、详情、保存和状态切换均经 `requireAdmin()`；已测试底层 guest/user/root 边界，真实 Telefunc 入口测试待补齐。
- [-] **5.2 完整服务端输入校验**：商品保存和列表查询路径已校验请求对象、ID、枚举、字符串长度、分类、Slug、封面 URL、布尔值、金额、库存、排序、分页和购买范围；仍需通过管理接口专项测试覆盖数据库业务边界。
- [x] **5.3 实现发布完整性校验**：状态切换到上架前重新读取并验证分类、Slug、发货条件和购买范围。
- [x] **5.4 规范并发状态切换**：将原状态放入数据库更新条件，并根据 `returning()` 判断并发结果。
- [-] **5.5 使用稳定 `AppError`**：商品列表、详情、保存、状态切换与同文件分类管理的预期失败均使用稳定 `AppError`；商品/分类必填与超长文本不再动态拼接错误码。独立库存模块和其他领域接口仍待统一。
- [x] **5.6 更新错误中文映射**：新增商品与分类输入错误码均已在 `lib/error-messages.ts` 映射。
- [x] **5.7 测试表单纯函数**：已覆盖默认值、详情转换、金额转换、条件字段、购买范围和中文/英文 Slug 生成。
- [x] **5.8 测试富文本清洗**：已覆盖脚本、事件属性、iframe、style/class、Base64 图片及允许内容保留。
- [-] **5.9 测试管理三身份**：已覆盖 `assertAdminAccess()` 的 guest 拒绝、user 拒绝和 root 成功；管理 Telefunc 结合 D1 的真实入口测试待补齐。
- [ ] **5.10 测试列表契约**：覆盖分页校正、搜索、筛选、总数和返回字段白名单。
- [ ] **5.11 测试保存业务规则**：覆盖 Slug 冲突、分类无效、金额和购买范围、固定内容上架及库存边界。
- [-] **5.12 测试错误脱敏**：已测试稳定商品错误映射及未知 SQL/栈类异常回退到统一文案，不向客户端暴露原始错误；Workers Observability 验证待部署环境完成。

## 阶段 6：工程验证与最终验收（10 项）

- [x] **6.1 运行文件 diagnostics**：商品服务端文件、错误映射、商品列表页、编辑弹窗、公开商品页和新增 RadioGroup 均无 error/warning。
- [-] **6.2 运行相关测试**：全量测试已通过 61 项；商品表单、Slug、富文本清洗、错误脱敏和鉴权边界测试通过，catalog 管理 Telefunc 真实 D1 契约专项测试尚未补齐。
- [x] **6.3 运行 `bun run lint`**：已无 error/warning。公开商品页的 `v-html` 使用仅局部豁免，并注明其数据来自服务端 `sanitizeProductDescription()` 白名单清洗。
- [x] **6.4 运行 `bun run build`**：Vike 客户端、SSR 和 Cloudflare Worker 目标构建通过（本轮修改后复验）。
- [ ] **6.5 手动验证成功路径**：至少快速创建草稿、完整创建/编辑、上架和公开详情展示成功。
- [ ] **6.6 手动验证输入错误路径**：至少验证名称、金额、购买范围、固定内容和恶意富文本错误。
- [ ] **6.7 手动验证关闭确认路径**：遮罩、Escape 和取消在脏表单状态下不会丢失输入。
- [ ] **6.8 检查浏览器网络响应**：列表无描述和发货全文，详情接口只在 root 编辑时返回所需完整字段。
- [ ] **6.9 部署环境验证**：确认富文本 SSR、媒体代理图片和 Cloudflare Workers 运行正常。
- [ ] **6.10 验证 Observability**：服务端未预期异常可在 Workers Observability 排查，客户端只收到统一脱敏文案。

## 功能验收清单

- [ ] 商品管理仍只有 `/catalog/products` 一个正式页面。
- [ ] 快速添加和完整添加使用同一个 Dialog 和表单状态。
- [ ] 快速添加只能创建草稿。
- [ ] 编辑商品时单独加载完整详情，不依赖列表全文数据。
- [ ] 商品表单使用 VeeValidate、Zod 和 shadcn-vue Field。
- [ ] 商品描述支持受限富文本和媒体库图片。
- [ ] 固定发货内容、发货提示和购买说明保持纯文本。
- [ ] 保存成功关闭 Dialog、Toast 提示并刷新列表。
- [ ] 脏表单不能通过遮罩或 Escape 意外关闭。
- [ ] 四种发货方式的条件字段和服务端保存规则正确。
- [ ] 商品详情安全展示富文本，不显示固定发货内容。
- [ ] 商品列表使用服务端搜索、筛选、分页和总数。

## 安全验收清单

- [ ] guest 和普通 user 不能读取或修改商品管理数据。
- [ ] 客户端校验无法替代或绕过服务端校验。
- [ ] `<script>`、事件属性、iframe 和危险 URL 不会保存或执行。
- [ ] 富文本图片不接受 Base64 和危险协议。
- [ ] 列表响应不包含完整描述、固定发货内容和发货提示。
- [ ] 固定发货内容不会出现在公开商品详情。
- [ ] 数据库异常、SQL、堆栈和原始清洗错误不会显示给客户端。
- [ ] 未预期异常进入 Workers Observability。

## 编码规范验收清单

- [ ] 页面复用 `AdminPageHeader`、`AdminDataTable`、`Pagination`、`MediaPickerDialog` 和现有 UI 组件。
- [ ] 业务页面没有新增原生 `input`、`select`、`textarea`、`button` 或 radio。
- [ ] 新增/编辑使用同一个 Dialog，不在列表下插入长表单。
- [ ] Dialog 内容区滚动、Footer 固定且窄屏可用。
- [ ] 所有字段具有 Label、错误提示和正确 `aria-invalid`。
- [ ] `Select`、`Switch` 和 `RadioGroup` 按 shadcn-vue 规范绑定。
- [ ] 临时反馈使用 Sonner Toast，不同时显示 Alert 和 Toast。
- [ ] 子组件不负责领域鉴权或直接调用商品业务 Telefunc。
- [ ] 没有新增重复导航、别名路由或页面私有标题映射。
- [ ] 若富文本编辑器未来提升为共享组件，已同步更新 `docs/components.md`。

## 待决事项

以下事项不阻塞设计，进入对应开发阶段时用构建和测试结果确认：

- [x] **HTML 清洗实现选择**：已实现并测试针对有限标签集的严格 Workers 兼容白名单清洗器。
- [x] **金额表单值类型**：已使用最多两位小数的字符串表单值，并在提交时转换为安全整数分。
- [x] **卡密库存摘要接口**：已合并到单商品管理详情，仅返回自动卡密商品的可用数量，不返回卡密全文。
- [x] **公开购买说明位置**：已在公开商品详情的商品信息区持续展示购买说明和人工/物流发货提示，保持纯文本边界。
